node_modules/
.wrangler/
.dev.vars
*.log

# Se genera en cada despliegue, no hace falta versionarlo
data/cr.json
cards.sql

# Ojo: NO añadas aquí nada que esté dentro de public/. Wrangler construye el
# manifiesto de assets desde esa carpeta y conviene que vea todo lo que hay,
# incluido lo que genera tools/build_site.py en cada despliegue.
